# راهنمای توسعه کافه صدا

این سند مرجع توسعه‌دهندگان برای اجرای محلی، شناخت معماری، توسعه API و رابط کاربری و حفظ کیفیت پروژه است.

## پیش‌نیازها

- Node.js `>= 20`
- npm `>= 10`
- Git
- Docker Desktop یا Docker Engine برای تست Production

## نصب و اجرای محلی

```bash
git clone https://github.com/mohamadkheiry/coffeshop.git
cd coffeshop
cp .env.example .env
npm ci
npm run dev
```

دستور `npm run dev` فرانت‌اند Vite را روی پورت `5173` و API را روی پورت `3000` اجرا می‌کند. Vite مسیرهای `/api`، `/uploads` و `/health` را به Express پراکسی می‌کند.

متغیرهای مهم `.env`:

| متغیر | کاربرد |
|---|---|
| `NODE_ENV` | در توسعه `development` و در سرور `production` |
| `PORT` | پورت Express؛ پیش‌فرض `3000` |
| `PUBLIC_URL` | آدرس Canonical و مبنای Sitemap/Open Graph |
| `JWT_SECRET` | امضای نشست مدیریت؛ حداقل ۳۲ کاراکتر تصادفی |
| `ADMIN_USERNAME` | نام کاربری مدیر اولیه |
| `ADMIN_PASSWORD` | رمز مدیر؛ در هر شروع، هش آن در مخزن کاربران همگام می‌شود |
| `TRUST_PROXY` | تعداد پراکسی‌های مورد اعتماد؛ پشت Nginx برابر `1` |
| `COOKIE_SECURE` | برای HTTP داخلی `false` و بلافاصله پس از فعال‌سازی HTTPS برابر `true` |
| `BIND_ADDRESS` | آدرس Bind پورت Docker؛ پشت Nginx برابر `127.0.0.1` و برای تست LAN برابر `0.0.0.0` |
| `APP_PORT` | پورت منتشرشده Docker روی میزبان؛ پیش‌فرض `8088` |

## چرخه داده

1. `server/data/seed.json` محتوای اولیه و نسخه‌بندی‌شده پروژه است.
2. در اولین اجرا، `content.json` از روی Seed ساخته می‌شود.
3. تغییرات پنل مدیریت فقط در `content.json` ذخیره می‌شوند و Seed را تغییر نمی‌دهند.
4. نوشتن محتوا اتمیک و صف‌بندی‌شده است تا فایل نیمه‌کاره یا تداخل ساده درخواست‌ها ایجاد نشود.
5. در Docker، پوشه‌های `server/data` و `server/uploads` روی Volumeهای پایدار قرار می‌گیرند.

برای تغییر محتوای پیش‌فرض نصب‌های جدید، `seed.json` را ویرایش کنید. برای تغییر داده یک محیط موجود از پنل مدیریت استفاده کنید یا پس از پشتیبان‌گیری، `content.json` همان محیط را اصلاح کنید.

## مدل محتوا

- `site`: عنوان Hero، داستان، تلفن، آدرس، ساعت کاری و شبکه اجتماعی
- `seo`: عنوان، توضیحات، کلمات کلیدی و تصویر Open Graph
- `categories`: شناسه پایدار، نام نمایشی و ترتیب
- `menuItems`: نام، توضیح، قیمت به تومان، دسته‌بندی، موجودی و ویژگی ویژه
- `media`: نوع `image|video`، عنوان، URL، Poster و ترتیب

قیمت‌ها عدد صحیح و به تومان ذخیره می‌شوند. نمایش اعداد با Locale فارسی در کلاینت انجام می‌شود.

## API

### عمومی

| متد | مسیر | توضیح |
|---|---|---|
| `GET` | `/api/content` | محتوای کامل سایت |
| `GET` | `/health` | سلامت سرویس |
| `GET` | `/robots.txt` | سیاست Crawler پویا |
| `GET` | `/sitemap.xml` | نقشه سایت با `PUBLIC_URL` |

### احراز هویت

| متد | مسیر | توضیح |
|---|---|---|
| `POST` | `/api/auth/login` | ایجاد کوکی نشست HttpOnly |
| `GET` | `/api/auth/me` | اعتبارسنجی نشست |
| `POST` | `/api/auth/logout` | حذف نشست |

### مدیریت (نیازمند نشست)

- `GET /api/admin/content`
- `PUT /api/admin/site`
- `PUT /api/admin/seo`
- `POST|PUT|DELETE /api/admin/categories[/id]`
- `POST|PUT|DELETE /api/admin/menu-items[/id]`
- `POST|DELETE /api/admin/media[/id]`

آپلود رسانه به‌صورت `multipart/form-data` با فیلد `file` انجام می‌شود. MIME باید تصویر یا ویدیو باشد و سقف فایل ۵۰ مگابایت است. تصویر با `sharp` چرخانده، حداکثر تا ۱۹۲۰ پیکسل کوچک و با کیفیت ۸۴ به WebP تبدیل می‌شود.

## فرانت‌اند

- مسیر `/` به‌صورت مستقیم بارگذاری می‌شود و محتوای عمومی را یک بار دریافت می‌کند.
- صفحات مدیریت با `lazy()` جدا شده‌اند تا JS پنل وارد Bundle اصلی بازدیدکننده نشود.
- کنترل‌ها، متن‌ها و فرم‌ها کدنویسی‌شده و قابل دسترس‌اند؛ تصاویر فقط نقش بصری دارند.
- Breakpointهای اصلی در `site.css` و `admin.css` برابر ۱۱۰۰، ۸۰۰/۷۶۰ و ۵۲۰ پیکسل هستند.
- حرکت‌ها با `prefers-reduced-motion` خاموش می‌شوند.
- برای اجتناب از رندر غیرضروری، داده‌های مشتق‌شده مانند Category Map با `useMemo` ساخته می‌شوند و صفحات مدیریت Dynamic Import هستند.

## زبان طراحی نسخه دوم

توکن‌های اصلی در `client/src/styles/global.css` تعریف شده‌اند:

- پس‌زمینه عمومی: `#0b0a08`
- متن و سطح عاجی: `#f0e8da`
- تأکید مسی: `#c7773c`
- تأکید زیتونی: `#76806a`
- فونت: Vazirmatn با fallback محلی

مراجع پذیرفته‌شده نسخه دوم در `docs/design/concept-v2-*.png` نگهداری می‌شوند. ترتیب تصویر و متن، تضاد مشکی/عاجی، خطوط موج مسی، تایپوگرافی درشت و قاب‌های چهارگوش رسانه اجزای اصلی هویت‌اند. تصویر کانسپت را برش نزنید و در UI استفاده نکنید؛ دارایی تولیدی مستقل را به WebP تبدیل کنید و UI، متن و کنترل‌ها را کدنویسی‌شده نگه دارید.

مرجع ایده لوگو `docs/design/concept-logo-v1.png` است؛ فایل اجرایی باید برداری باقی بماند. نشان از دایره فنجان/وینیل، دسته کوچک، موج سه‌قله در فضای منفی و نقطه مرکزی تشکیل شده است. نسبت اجزا یا ضخامت موج را بدون بررسی در اندازه ۱۶ و ۳۲ پیکسل تغییر ندهید. برای بازتولید PNGهای Favicon می‌توان از `sharp` و فایل `client/public/favicon.svg` به‌عنوان منبع استفاده کرد.

در تغییرات Responsive، موبایل را به‌عنوان چیدمان مستقل بررسی کنید: منوی تمام‌صفحه، Hero پایین‌چین، نوار دسته‌بندی قابل اسکرول، تصویر منوی افقی، موزاییک رسانه تک‌ستونه و جدول تماس عمودی. کارت‌های تکراری، Glow/Neon، Glassmorphism، گوشه‌های گرد بی‌دلیل، Badgeهای تزئینی و متن ریز با این زبان هم‌خوان نیستند.

## کیفیت، تست و امنیت

پیش از Commit:

```bash
npm run check
npm audit --omit=dev
docker compose build
```

`npm run check` شامل ESLint، تست‌های Node و Build تولیدی است. برای تغییرات UI علاوه بر Build، این مسیرها را دستی یا با مرورگر خودکار بررسی کنید:

1. بارگذاری صفحه اصلی و پیمایش تا تمام Sectionها
2. تعویض دسته‌بندی منو
3. باز و بسته کردن Lightbox رسانه
4. ورود به `/admin`
5. افزودن، ویرایش و حذف یک آیتم آزمایشی
6. آپلود تصویر و ویدیو، مشاهده در سایت عمومی و حذف آن
7. ذخیره محتوای سایت و SEO
8. تست عرض موبایل ۳۹۰ پیکسل و دسکتاپ ۱۴۴۰ پیکسل

هر Route مدیریت باید پشت `requireAuth` بماند. فایل‌های کاربر را با نام اصلی روی دیسک ذخیره نکنید؛ شناسه UUID و پسوند کنترل‌شده فعلی را حفظ کنید. Secrets را Commit نکنید و `.env` را در لاگ یا Issue منتشر نکنید.

## افزودن قابلیت جدید

1. مدل داده را در Seed و اعتبارسنجی API تعریف کنید.
2. Route را با Error Handling و در صورت نیاز `requireAuth` اضافه کنید.
3. UI را به‌صورت Component کوچک و مستقل بسازید؛ `App` فقط مسئول ترکیب Routeها بماند.
4. حالت Loading، خطا، Empty State، موبایل و دسترس‌پذیری Keyboard را پوشش دهید.
5. تست واحد برای منطق Server و تست مرورگر برای مسیر کاربر اضافه کنید.
6. اسناد API و استقرار را در همین تغییر به‌روز کنید.

## عیب‌یابی

- خطای 401 در پنل: کوکی منقضی شده؛ دوباره وارد شوید و زمان سیستم را بررسی کنید.
- تصویر آپلود نمی‌شود: MIME، سقف ۵۰MB و دسترسی نوشتن `server/uploads` را بررسی کنید.
- تغییرات پس از Restart از بین می‌رود: مطمئن شوید Volumeهای Compose حذف نشده‌اند.
- متادیتای اشتباه: `PUBLIC_URL` و مقادیر SEO پنل را بررسی و Container را Restart کنید.
- Build روی Windows: دستورات npm را از PowerShell یا Git Bash و در Root پروژه اجرا کنید.
